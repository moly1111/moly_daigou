# 硬件端配置示例（复制为 device_config.txt 后填写）
# 设备无法读取服务器 .env，所以 API Key 必须存在设备本地（本脚本用此文件模拟）
# 第一行：API Key（与服务器 .env 中 RFID_API_KEY 一致，由管理员下发到设备）
# 第二行：服务器地址（可选，不写则默认 http://127.0.0.1:5000）
h92hsdcj2eigh929fh29ck920909h
http://127.0.0.1:5000
